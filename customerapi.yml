apiVersion: apps/v1
kind: Deployment
metadata:
  name: customer-service
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: customer-service
  template:
    metadata:
      labels:
        app: customer-service
    spec:
      containers:
      - name: customer-service
        image: emilwitt/onlineretailerpartial2-customer-service:latest 
---
apiVersion: v1
kind: Service
metadata:
  name: customer-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: customer-service
  ports:
  - port: 80
    nodePort: 31004