apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-service
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: product-service
  template:
    metadata:
      labels:
        app: product-service
    spec:
      containers:
      - name: product-service
        image: emilwitt/onlineretailerpartial2-product-service:latest 
---
apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: product-service
  ports:
  - port: 80
    nodePort: 31005
